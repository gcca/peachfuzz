services:
  peachfuzz:
    build:
      context: .
      dockerfile: Dockerfile
      target: execute
    image: peachfuzz:latest
    environment:
      TZ: UTC
      DB_URL: /app/data/peachfuzz.db
      LOAD_SAMPLE_DATA: "1"
    ports:
      - "8000:8000"
    volumes:
      - db:/app/data
    restart: unless-stopped

volumes:
  db:
